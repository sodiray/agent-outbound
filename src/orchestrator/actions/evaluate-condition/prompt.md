Evaluate whether a condition passes for this record.

Condition:
{{condition_text}}

Record:
{{record_json}}

Step outputs:
{{step_outputs_json}}

Return JSON with:
- passed: boolean
- defer: boolean
- reason: string
